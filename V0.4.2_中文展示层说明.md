# v0.4.2 中文展示层覆盖说明

这一版同时解决两个用户可见问题：

1. 技能列表只保留一个总控，并把可见名称改成 **知识整理总控**。
2. 正常整理结果不再出现 `external=外部资料`、`family_builder`、`method-xxx`、`organized-xxx` 这类机器语言。

## 覆盖方式

1. 建议先提交 Git 或复制一份原项目。
2. 把本压缩包内容复制到原 `knowledge-system` 根目录，选择合并/覆盖同名文件。
3. 如果你之前还没有完成“多 Skill → 单总控”迁移，双击：

```text
install_v04_single_orchestrator.bat
```

4. 重启或刷新宿主的 Skill 列表。

如果你已经安装过 v0.4.1，原来的 8 个 specialist 已经是 `STAGE.md`，这次通常只需要覆盖文件并刷新技能即可。

## 你以后应该看到什么

技能列表里，这一组只显示：

**知识整理总控**

正常回答里的类型说明直接写：

**知识内容、项目案例、文案素材、观点灵感、待办行动、项目方案、外部资料、待整理**

不再写：

```text
external=外部资料; case=项目案例; copy=文案素材...
```

正常步骤显示使用中文，例如：

```text
已完成：同类知识归组
下一步：冲突与版本检查
```

而不是：

```text
family_builder completed
reconciler
```

正常知识条目也不再直接显示：

```text
method-reverse-topic-selection
business-ip-store-stall-validation
organized-20260913-7-a84d19
```

系统应从条目的标题、问题、摘要、主张或正文中使用中文名称；如果没有标题，就根据实际内容生成一个简短中文标题。内部 ID 仍保留在数据层，用于引用和兼容，但默认不展示。

## 调试例外

只有你明确要求“显示 ID / 技术字段 / 调试信息”时，才允许单独展示内部 stage、asset type、slug、run_id、artifact_id 等机器字段。

## 新增/修改文件

- `skills/knowledge-orchestrator/SKILL.md`：可见名称改为“知识整理总控”，加入强制中文展示规则。
- `skills/knowledge-orchestrator/stages.json`：每个内部阶段增加 `label_zh`。
- `src/display-labels.js`：集中管理类型、阶段、状态、动作、Gate、关系的中文显示名，并识别需要隐藏的内部 ID。
- `src/agent-runtime.js`：给内部执行单元加入“语义字段用中文、机器 ID 不得当标题”的规则，并返回中文 display 摘要。
- `src/stage-registry.js`：阶段指令同时带中文显示名。
- `src/workflow-cli.js`：新增 `view` 命令，输出中文状态摘要，不改动原机器命令的兼容性。
- `tests/display-labels.test.js`：验证中文类型、阶段映射和内部 ID 隐藏。

## 兼容原则

这次**不修改历史 ID、不修改英文 Schema 枚举、不批量重写旧 RUN_STATE/Artifact**。因此原来的引用链和历史运行仍然可恢复。变化只发生在“给人看”的展示层和未来生成的中文语义标题上。
