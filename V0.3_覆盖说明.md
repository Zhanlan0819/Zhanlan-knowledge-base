# knowledge-system v0.3 覆盖包

这个压缩包按项目根目录组织。解压后，把其中 `src/`、`schemas/`、`skills/`、`tests/`、`scripts/` 以及根目录 Markdown 直接复制到原 `knowledge-system` 根目录，选择“覆盖同名文件 / 合并目录”。

## 本次修改

1. 新增 `src/agent-runtime.js`：真正执行 `resume → 当前阶段 → 当前 specialist → 模型 → submit/retry`；Gate/正式写入/终态自动停止。
2. 新增 `src/stage-registry.js`：按阶段加载唯一 specialist Skill 和最小必要上下文；避免把九份 Skill 全塞给模型。
3. 新增 `src/review-crypto.js`：推荐 Ed25519，Review Service 用私钥签名，Agent Runtime 只持公钥验签；旧 HMAC `KB_REVIEW_SECRET` 兼容。
4. 修改 `src/workflow.js`：
   - 新 run 为 `schema_version=0.3.0`；旧 0.2.0 仍可验证。
   - `resume()` 返回 `required_action`。
   - 正式审批/rollback/apply/publish 除 actor 外还必须具备签名能力。
   - Artifact 支持 provenance（Skill SHA、模型、runner、attempt、context SHA）。
5. 修改 `src/workflow-cli.js`：
   - 支持公钥/私钥文件。
   - 新增 `prepare-stage`、`run-stage`、`run-until-stop`。
6. 收紧 `schemas/workflow-state.schema.json`：固定 14 个阶段和合法 `current_stage`，同时兼容 0.2/0.3。
7. 扩展 `schemas/artifact-envelope.schema.json`：provenance 可选，旧 Artifact 兼容。
8. 更新 orchestrator、README、阶段契约，并增加 Runtime/Ed25519/溯源测试。

## 覆盖前建议

先复制一份原项目，或者确保 Git 当前工作区已提交。这个包不删除任何原文件，也不包含 `node_modules`。

本包**没有覆盖**你原项目中的以下文件，因为这次没有修改它们：

- `schemas/run.schema.json`
- `schemas/skill-candidate.schema.json`
- 八份 specialist `SKILL.md`
- `package.json`
- fixtures / examples / v0.1 CLI

这些文件必须继续保留在原项目中。

## 第一次运行

覆盖后先在原项目根目录运行：

```powershell
npm test
```

然后可先检查普通状态：

```powershell
node src/workflow-cli.js resume --store <store目录> --run <wf_id>
```

输出现在会多一个 `required_action`。

## 推荐启用新的审阅签名

生成 Ed25519 密钥：

```powershell
node scripts/generate-review-keypair.js --out .review-keys
```

普通 Agent / Runtime 进程只设置公钥：

```powershell
$env:KB_REVIEW_PUBLIC_KEY_FILE=".review-keys/review-public.pem"
```

真人审阅终端在执行 `decision/rollback/apply/publish` 时再设置私钥：

```powershell
$env:KB_REVIEW_PRIVATE_KEY_FILE=".review-keys/review-private.pem"
```

不要把私钥文件放进模型上下文、Git 仓库或公开目录。

旧 run 如果已有 HMAC 审批，继续设置原来的：

```powershell
$env:KB_REVIEW_SECRET="原来的secret"
```

直到这些旧 run 结束。新 run 建议改用 Ed25519。

## Agent Runtime 三种用法

### 1. 最安全：只准备当前阶段请求

```powershell
node src/workflow-cli.js prepare-stage --store <store> --run <wf_id>
```

它只输出当前一个阶段的请求包，不会自动推进下一阶段。宿主 AI 完成后，可继续使用原来的 `submit/retry`。

### 2. 执行一个阶段

先把你的模型/Agent CLI 包装成“stdin 接收 JSON、stdout 只返回 JSON”的命令，然后：

```powershell
$env:KB_MODEL_COMMAND="你的模型命令"
$env:KB_MODEL_ARGS_JSON='["参数1","参数2"]'
$env:KB_MODEL_ID="my-model"
node src/workflow-cli.js run-stage --store <store> --run <wf_id>
```

### 3. 自动跑到下一个 Gate / 正式写入 / 终态

```powershell
node src/workflow-cli.js run-until-stop --store <store> --run <wf_id> --max-steps 20
```

这个命令不会越过真人 Gate，也不会自动执行 Canonical/正式 Skill 写入。

## 模型桥接协议

Runtime 给外部模型命令的 stdin 是：

```json
{
  "protocol": "knowledge-stage-request/v0.3",
  "stage": "family_builder",
  "rules": ["..."],
  "skill": {"path": "...", "sha256": "...", "content": "..."},
  "output_contract": {"schema_ref": "...", "stage_schema": {}},
  "context": {}
}
```

模型命令 stdout 必须是：

```json
{ "families": [] }
```

或：

```json
{ "output": { "families": [] } }
```

不要混入 Markdown 代码围栏、解释文本或日志；日志请写 stderr。

## 兼容性

- v0.2 `RUN_STATE.json`：继续支持。
- v0.2 HMAC decisions：继续支持，但恢复时仍需旧 `KB_REVIEW_SECRET`。
- v0.2 Artifact：没有 provenance 也继续支持。
- v0.3 新 run：状态版本为 0.3.0。
- `pipeline.js` 数据形状本次未改变。

## 本次本地验证范围

已完成：所有新增/修改 JS 的 `node --check` 语法检查；JSON Schema 文件可解析；Ed25519 签名/仅公钥验签的独立运行检查通过。

由于你给我的交接包没有包含原项目的 `schemas/run.schema.json`、`schemas/skill-candidate.schema.json`、fixtures、package.json/node_modules，我无法在这里真实执行完整 `npm test`。因此覆盖后第一步务必在**原项目完整目录**运行 `npm test`。本包新增了测试，原 26 项也应一起执行。
