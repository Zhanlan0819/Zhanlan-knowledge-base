# v0.4 单总控覆盖包

这一版解决你截图里的问题：**「知识整理总控」是唯一总控；Router / Distiller / Evaluator 等不再作为平级 Skill 出现在宿主技能列表。**

## 你只需要做 3 步

1. 先备份项目或 Git commit。
2. 把本压缩包内容复制到原 `knowledge-system` 根目录，合并文件夹并覆盖同名文件。
3. 双击根目录的 `安装_v0.4_单总控.bat`。

第三步必须做，因为 ZIP 覆盖只能新增/替换文件，**不能自动删除旧的 specialist `SKILL.md`**。批处理会安全迁移：

```text
skills/knowledge-router/SKILL.md
→
skills/knowledge-router/STAGE.md
```

其它七个 specialist 同理。迁移前会在 `_skill-migration-backup/<时间>/` 留原文件备份。

## 安装后检查

运行：

```powershell
node src/workflow-cli.js skill-visibility
```

正确结果应满足：

```json
{
  "visible_specialist_skill_files": [],
  "single_orchestrator_ready": true
}
```

然后重启/刷新宿主 Skill 列表。此时这套知识系统应该只剩 **知识整理总控** 一个入口。你其它独立 Skill（例如 Axing Knowledge）不会被脚本处理。

## 新的内部结构

```text
知识整理总控 (SKILL.md)              ← 用户只调用这个
        ↓
WorkflowService.resume()
        ↓
required_action + next_stage
        ↓
stages.json
        ↓
当前唯一 STAGE.md
        ↓
AgentRuntime
        ↓
模型结构化 JSON
        ↓
WorkflowService.submit/retry
        ↓
Gate 时 STOP / 否则继续
```

因此总控负责“计划和调度”，内部阶段负责“当前这一小步怎么做”，WorkflowService 负责“能不能做、能不能跳、能不能写”。

## 本轮保留的 v0.3 改进

- `required_action`：execute / retry / review / apply / publish / done。
- Agent Runtime：只执行当前阶段。
- 最小上下文：不把全部阶段提示词一起塞进模型。
- Ed25519：Agent 只拿公钥，Review Service 才拿私钥。
- Artifact provenance：记录阶段指令、模型、attempt、context 摘要。
- 旧 v0.2/v0.3 状态继续兼容。

## 可恢复旧结构

如果你想撤回“单总控显示”，双击：

`恢复_多Skill显示.bat`

它会把 `STAGE.md` 改回 `SKILL.md`。Runtime 同时支持旧文件作为兼容 fallback。

## 覆盖后测试

先执行：

```powershell
npm test
```

再执行：

```powershell
node src/workflow-cli.js skill-visibility
```

本包新增了单总控 Stage Registry / Agent Runtime 测试；原 workflow 测试也保留。

## 我这里完成的验证

- 新增/修改 JS 全部通过 `node --check`。
- JSON 文件通过解析检查。
- `stage-registry.test.js` 与 `agent-runtime.test.js` 可在本包独立运行。
- 迁移脚本完成 apply/restore 临时目录实测。
- Ed25519 私钥签名/公钥验签独立检查通过。

由于你最初交接给我的 8 文件包没有 `schemas/run.schema.json`、`schemas/skill-candidate.schema.json`、fixtures、package.json/node_modules，我不能在这里完整执行原项目全部 `npm test`。所以最终完整回归测试仍要在你的原项目覆盖后运行。
