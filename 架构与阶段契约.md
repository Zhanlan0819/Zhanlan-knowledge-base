# v0.2 工作流状态机、阶段产物与审批边界

本阶段在 v0.1 的九模块、RAW/资产/Claim/Family/Theme/Proposal 数据和演示纵切之上增加持久状态机。四份参考仓库及 [原研究映射](研究与映射.md) 不重做。新增机制属于**本项目适配层**；仓颉的阶段状态、产物和 Gate 思想只是依据，不意味着仓颉已实现这个混合资产知识库。

## 运行持久化

每次 `start` 新建独立 `wf_<UUID>`，即便输入 RAW 相同也有不同 workflow run；相同 RAW 按哈希复用只新增原件。目录形态：

```text
store/
  raw/<raw_id>.txt                  原始 UTF-8 字节，只新增
  raw/<raw_id>.json                 完整 SHA-256 元数据
  runs/<旧版建议run_id>.json         v0.1 的非正式建议快照，保留兼容
  workflows/<wf_id>/
    RUN_STATE.json                 当前状态（原子替换）
    state-history/*.json           只新增的旧状态快照
    AUDIT.jsonl                    追加日志
    artifacts/<stage>/vN.json      每阶段带输入指纹的版本产物，只新增
    evaluation-outputs/<id>.json   Skill/baseline/改前改后任务输出字节与摘要，只新增
  canonical/<canonical_id>.json    仅受保护审批服务写入，版本只新增
  formal-skills/<id>.json          仅受保护发布服务写入的发布记录
```

`RUN_STATE` 的 Schema 是 [workflow-state.schema.json](schemas/workflow-state.schema.json)。每阶段记录 status、version、artifact_id、SHA-256、error；每个 Artifact envelope 记录 run/stage/version/producer/created_at、依赖阶段的 artifact_id+SHA-256 和实际数据。新会话 `resume` 重新读取文件，核 RAW、阶段产物、当前依赖指纹、签署审批；不依赖对话记忆。

## 阶段依赖与分支

```text
intake → router ┬→ case/copy/idea/todo/project/external/unorganized：资产终点
                └→ knowledge → atomicizer → claims → family_builder
                                  → theme_builder → reconciler → distiller
                                  → proposal → [Gate D] → canonical
                                  └（批准且成熟者）→ skill_candidate → [Gate E]
                                    → skill_builder → evaluator → [Gate F]
                                    → skill_publish
```

Gate A 位于高风险 router 后；Gate B 位于重大主题变化后；Gate C 位于冲突/版本判断后。没有 knowledge 时，router 完成后剩余知识/Skill 阶段为 `not_applicable`，运行结束为 `completed_nonknowledge`。没有 Skill Candidate 时，Skill Builder/Evaluator/发布为 `not_applicable`；不把所有资产强制带进长流水线。

依赖图是显式常量 `src/workflow.js` 中 `DEPENDS`。每个 Artifact 的 `inputs[]` 写入当时的上游 ID/摘要。修改 Family 等工作产物应使用 rollback+retry 创建新版本，**不要直接编辑 vN.json**（会触发篡改错误）。人工 rollback 上游阶段时，该阶段置 `needs_revision`，所有后续阶段置 `stale`，历史 Artifact 保留但不能作为当前有效产物。重做上游后，下游必须逐阶段重交且重新验收；旧 Gate 批准不能套到新产物（审批绑定 `source_artifact_id`）。RAW 和已发布 Canonical 不做破坏性 rollback：错误只能再提修订 Proposal。

## Artifact Contract

全部数据形状在 [workflow-artifacts.schema.json](schemas/workflow-artifacts.schema.json) 的各阶段 `$defs`；外壳在 [artifact-envelope.schema.json](schemas/artifact-envelope.schema.json)。通用完成条件：JSON 可解析、对应 Schema 通过、输出 ID 唯一、引用存在且输入指纹匹配、该阶段 `validateLinks` 的关系/覆盖约束通过。只有 `generate → validate → commit` 成功后才标 completed/waiting_review；失败标 `failed` 并写错误审计。文件存在绝不是完成条件。

| 阶段 | 输入与产物数据 | 附加完成条件 / 失败条件 | 下一步前置与 Gate |
|---|---|---|---|
| intake | UTF-8 bytes → `{raw_id,sha256,bytes}` | RAW 精确字节只新增；同 ID 再导入核完整 SHA；非法 UTF-8/空输入失败 | router 需 intake completed |
| router | intake RAW → `{assets,risk_flags}` | 每个引用精确落在 RAW；所有非空白 UTF-16 位置至少有一个资产覆盖；ID 唯一、raw_id 正确；漏段/虚构段失败 | 高风险旗标或 `unorganized` → Gate A；批准后才能向后 |
| atomicizer | router 中 knowledge 资产 → `{atoms,unresolved_asset_ids?}` | 每个 knowledge asset 恰好一 atom；原文锚点/raw_id/陈述不改；未处理项非空或漏映射失败 | claims 需 atomicizer completed |
| claims | atoms → `{claims}` | 每个 atom 恰好一 Claim；atom/raw/引用/statement 一致；孤立或少返回失败 | family_builder 需 claims completed |
| family_builder | Claims → `{families}` | Claim 恰好属于一 Family；重复、遗漏、断链失败；同主张语义正确性仍需模型/人审 | theme_builder 需 family_builder completed |
| theme_builder | Families → `{themes,major_change}` | 所有 Family 有主题；每主题有中心问题、纳入/排除、真实 Family IDs；缺字段/孤立 Family 失败 | `new_core/merge_core/split_core` → Gate B；reconciler 需批准 |
| reconciler | Themes + Claims → `{relations,version_judgement}` | 端点均为独立 Claim；冲突/撤回需非空判断依据；端点断链失败 | CONTRADICTS/RETRACTS/版本判断 → Gate C；distiller 需批准 |
| distiller | Theme/Family/Claim/RAW → `{distillations}` | 每 Theme 一份；支持 Claim 必须来自对应 Theme 的 Family；漏主题/外来证据失败 | proposal 需 distiller completed |
| proposal | distillations/Claims → `{proposals}` | 每份蒸馏关联可审阅 Proposal；双方冲突引用都须存在；本链只接受 `canonical_knowledge` 目标；缺提案或断链失败 | Gate D 必停；没有签署决定不得进 canonical |
| canonical | 批准的 Proposal + 决定 → `{canonical_versions}` | **不接受通用 `submit`**；服务检查 Gate D、Proposal 当前版本、签名、接受/部分接受文本；只新增，不覆盖 | skill_candidate 仅在 canonical completed 后 |
| skill_candidate | Canonical → `{candidates}` | Canonical/Claim 来源真实；仓颉三重验证+独立晋级门+RIA 结构必须通过；失败时仍留知识 | 非空候选 → Gate E；未批准不得建 Skill 草稿 |
| skill_builder | 晋级候选 → `{drafts}` | 每草稿绑定已晋级 candidate，Schema 通过；不写正式 Skill | evaluator 需 skill_builder completed |
| evaluator | drafts/已登记任务输出 → `{evaluations}` | 每测试有可读取、可核 SHA 的 Skill 输出与无 Skill baseline 输出；同一组任务覆盖齐；改前/改后也有输出记录；奇数且不重复的 judge ID；建议与投票一致；keep 时声明的断言全通过 | keep → Gate F；worse 多数 → draft `needs_revision`、后续 stale |
| skill_publish | Gate F + evaluator → `{formal_skills}` | **不接受通用 `submit`**；只由签署发布服务创建追加式发布记录；退步/未审不能发布 | 运行 completed；真正安装/运行仍需外部宿主 |

这些是**结构与引用完整性**门禁。`recordEvaluationOutput` 保存实际字节和摘要，`resume` 会重新核对，但本骨架还没有独立任务运行器，不能证明这些字节是指定模型真实完成的；`assertion_passed` 与 judge 独立性也未机器证明。Stage 输出是模型/外部工具候选，程序只验可确定的部分。

## 六个 Gate 与签署记录

| Gate | 自动触发条件 | 需要用户判定的事 |
|---|---|---|
| A `route_risk` | router 标高风险或出现 `unorganized` | 漏分流/敏感来源/高风险路由是否可继续 |
| B `major_theme_change` | 新核心主题/合并/拆分标记 | Theme 边界、旧主题影响 |
| C `conflict_or_version` | CONTRADICTS、RETRACTS 或版本判断 | 双方证据、时间/场景/张力、是否应修订 |
| D `canonical_proposal` | 有待确认 Proposal | 逐 Proposal 接受/拒绝/部分接受；正式知识仅由批准内容生成 |
| E `skill_promotion` | Skill Candidate 非空且门禁通过 | 是否值得产品化为独立 Skill |
| F `skill_publication` | 有 keep 建议的评测 | 优化版本是否允许形成发布记录 |

Checkpoint 在 `RUN_STATE.checkpoints` 有 `waiting_user_approval/approved/rejected`。结构化决定有 proposal_id、decision、approved_by/at、accepted/rejected_changes、**所审批的 source_artifact_id**、HMAC signature；留在 `RUN_STATE.decisions` 和审计日志。拒绝 Gate A/B/C/E/F 会把阶段退回 `needs_revision` 并使下游 stale；Gate D 全拒绝则运行 `rejected`、Canon 仍空；部分接受只把手工列出的 accepted_changes 写入新的 Canonical 版本。审批记录更改或“只改状态为 approved”会因签名/来源绑定验证失败。

CLI 审阅命令只在 TTY 且由外部提供 `KB_REVIEW_SECRET` 时开放；服务层也拒绝默认 `actor=ai` 的审批、RAW 覆盖、Canonical 直写和发布。**这不是 OS 级权限隔离**：如果 AI 与审阅服务共享同一进程权限、能读取审阅密钥或直接写磁盘，它仍可能绕过服务。生产化必须把审阅密钥与正式存储放在模型工具访问范围之外，并加用户身份认证、数据库事务/ACL、独立审计存储。

## 失败恢复与审计

- `resume`：载入 `RUN_STATE` 与当前 Artifact，验摘要、依赖、审批；返回需确认的 Gate 或最早可执行阶段。
- `retry`：仅 `failed/needs_revision/stale` 阶段可重交，同阶段产生 `vN+1`，保留旧产物；无需重采 RAW。
- `rollback`：人工把指定非正式阶段置 `needs_revision`，下游 stale；不删除历史。若 evaluator 的 paired 多数判 worse，自动退回 Skill 草稿并禁止发布。
- `AUDIT.jsonl`：记录 run、stage、输入/输出 ID、model/agent、时间、结果、错误、用户决定。`traceCanonical` 可返回 Canonical←Proposal←Theme←Family←Claim←RAW 链。日志目前追加在本地文件，不是抗管理员篡改的审计库。

## 未来 UI 状态（本阶段不改前端）

UI 每个运行显示标题、run_id、阶段图标、当前停点、证据/产物链接、下一动作。示例：

```text
智能整理 wf_...
✓ RAW 已接收             查看原件/摘要
✓ 内容分流               1 知识 · 1 待办 · 1 灵感 · 1 文案
✓ 知识原子与 Claim       查看原文锚点
✓ 观点族与主题簇         查看成员/纳入排除条件
✓ 关系与蒸馏             查看来源链
● Proposal：等待确认      接受 / 拒绝 / 部分接受（逐项）
○ Canonical              只有接受后才新增版本
○ Skill 孵化              默认不启动
```

点击任一失败/停点要给出具体缺失条件、Schema/引用错误、需要确认的对象、确认后允许执行的下一阶段。显示 `stale` 的下游项并可查看历史版本，但不能误标为“已完成”。未来 UI 调用受保护审阅服务，不能只是给聊天发一句“同意”。

## 从 Prompt 到代码的迁移与余留

| v0.1 主要靠模块规则提醒 | v0.2 已落地约束 | 仍需模型/人工 |
|---|---|---|
| “别跳阶段” | `DEPENDS` + completed/批准 Gate 前置拦截 | 阶段内容是否语义正确 |
| “别漏原文/知识” | router 全 RAW 覆盖、atom/Claim/Family/Theme 数量与引用检查 | 拆件粒度与类别是否合适 |
| “阶段做完了” | Schema+关系校验+版本 Artifact 才能推进；坏 JSON failed | 事实真伪、主张语境 |
| “记住当前进度” | RUN_STATE/快照/摘要/依赖指纹；新会话 resume | 有无遗漏的外部信源 |
| “别直接改正式知识/RAW” | 无通用写入接口；RAW `wx`+摘要；Canonical 审批服务+签署决定 | 生产 OS/账号级权限隔离未实现 |
| “上游改了要重算” | rollback 阶段 + 下游 stale + 重交 vN + Gate 绑定版本 | 哪些语义变化值得提案 |
| “Skill 不能乱发布” | 三重/晋级/RIA结构门、baseline/输出记录要求、paired/keep-revert、Gate E/F | 真假独立 judge 与真实任务质量仍须外部运行器/审校 |

当前可证明的是**服务 API 与运行状态的越级、引用、审批绕过会被拒绝**。不能证明模型一定识别全部冲突、一定给出正确 Family/Theme、审阅人身份真实或本地文件对拥有磁盘写权限的外部程序不可修改。本骨架不是生产级完成品。
