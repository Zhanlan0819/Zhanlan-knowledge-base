# 架构与阶段契约｜v0.7.0

## 1. 总原则

系统采用三层职责：

- Agent / 模型：理解语义、提出候选、形成用户可读结果。
- Workflow Runtime：负责阶段顺序、Schema、来源引用、状态、治理审计与可恢复执行。
- Review Service：负责真人审批签名、Canonical 正式写入和 Formal Skill 正式发布。

核心原则：

> 模型负责判断，程序负责纪律，用户只负责真正需要人的决定。

正常入口是 src/workflow-cli.js。它默认使用 src/workflow-v063.js，不应直接修改 RUN_STATE.json 或 Artifact 推进流程。

## 2. 主流程

~~~text
RAW
→ 内容分流：type × source_identity × topic_tags × verification_status
├─ 非知识资产 → Delivery lanes
└─ knowledge
   → 知识拆解
   → Claim
   → Family（语义同一性）
   → Theme 图谱（唯一主归属 + 可选跨主题引用）
   → 关系/冲突/版本
   → 知识蒸馏
   → Proposal
   → 真人审批
   → Canonical
   → 每条 Canonical 做 Skill 晋级审计
      ├─ 不晋级 → 保留知识 + 审计原因
      └─ 晋级
         → Skill Candidate
         → Skill Draft
         → 实际任务评测
         → 真人审批
         → 可安装 Formal Skill

全部通道
→ Delivery Bundle
~~~

Router 的非知识资产不是“流程外废料”。todo、memo、copy、idea、case、project、external、unorganized 都必须进入最终交付。

## 3. 14 个基础程序阶段

| 程序阶段 | 用户含义 | 关键完成条件 |
|---|---|---|
| intake | RAW 接收 | 原始字节只新增，摘要一致 |
| router | 内容分流 | RAW 非空白全覆盖；9 类 type 合法；source_identity / topic_tags / verification_status 独立记录；风险显式标记 |
| atomicizer | 知识拆解 | 每个 knowledge 至少一个 atom；锚点位于资产原文 |
| claims | 主张提取 | 每 atom 一 Claim；证据字段由 Runtime 从 Atom 复制 |
| family_builder | 同类知识归组 | 每 Claim 恰好一个 Family |
| theme_builder | 主题整理 | Family 全覆盖；每个 Family 恰好一个主 Theme，可被其它 Theme 次级引用；Theme 有单一中心问题/纳入/排除 |
| reconciler | 重复/补充/冲突 | 关系端点有效；冲突/撤回有依据 |
| distiller | 知识蒸馏 | Theme 全覆盖；有 user_brief 时必须通过目标覆盖审计 |
| proposal | 正式知识提案 | 蒸馏结果形成可审 Proposal |
| canonical | 正式知识 | 只能由 Review Service 追加写入 |
| skill_candidate | 能力候选 | 全部 Canonical 先有晋级审计；Candidate 通过 Triple Verify + Promotion + RIA++ |
| skill_builder | 能力规则构建 | 草稿绑定已批准 Candidate |
| evaluator | 实际任务评测 | baseline、with-skill 输出、paired judge 完整 |
| skill_publish | 正式能力发布 | Review Service 发布，并生成真实 SKILL.md 文件夹 |

## 4. Decision-First Review

内部阶段不等于人工检查点。没有需要用户判断的内容时，不应机械暂停。

硬 Gate：

| Gate | 触发条件 | 用户真正判断 |
|---|---|---|
| A route_risk | Router 风险或待整理 | 分流/敏感边界 |
| B major_theme_change | 核心主题新增、合并或拆分 | 高层结构是否接受 |
| C conflict_or_version | 冲突、撤回或版本取舍 | 哪个版本/边界应保留 |
| D canonical_proposal | 有 Proposal | 逐条接受、拒绝或部分接受 |
| E skill_promotion | 有 Candidate | 是否值得进入 Skill 构建/评测 |
| F skill_publication | 评测建议 keep | 是否正式发布 Skill |

Theme 还保留一个高价值软检查点；“继续”只通过软检查点，不等于正式审批。

## 5. Review UX

正常审核入口：

~~~powershell
node src/workflow-cli.js review --store <store> --run <run_id>
~~~

Review Panel 只监听 127.0.0.1。

- 普通 Gate：批准当前 Artifact / 退回修改。
- Proposal Gate：一页批量处理多个 Proposal。
- 批量审完可直接 apply Canonical。
- Gate F 可直接正式发布 Skill。

旧 decision/apply/publish/rollback 命令属于排障接口，不是默认用户界面。

### 签名语义

非 Proposal Gate 禁止签入 AI 自由文本 accepted/rejected。签名对象只表达“当前 Artifact + approved/rejected”。

Proposal：
- approved：accepted_changes 由 Runtime 直接取 proposal.text；
- rejected：Runtime 直接取 proposal.text 放入 rejected_changes；
- partial：只有此时用户明确填写最终接受文本。

这保证聊天表达、选择动作和审计记录不会因 AI 拼接命令而错位。

## 6. 状态与恢复

RUN_STATE.json 保存：

- run 状态
- 当前阶段
- 每阶段版本和 Artifact 活动指针
- Gate / Checkpoint
- 签署决定
- user_brief
- 软交互停点

历史 Artifact 与 state-history 追加保留。

### rollback 规则

v0.6.3 的活动状态转换：

~~~text
回退目标 → needs_revision
下游活动阶段 → not_started
旧下游 Artifact → 文件保留，但活动指针清空
~~~

因此重跑后可以自然继续到 Canonical，不需要手改 stale。

一旦 Canonical 已正式写入，禁止再回滚其上游去改写同一 run 的事实历史；应创建修订 Proposal/run。Formal Skill 发布后也禁止破坏性回滚。

## 7. v0.7 分类与主题图谱契约

### 7.1 Router 四轴

同一资产不得再靠一个分类字段同时表达来源与用途：

- `type`：未来主要交付通道；
- `source_identity`：来源身份；
- `topic_tags`：多选检索标签；
- `verification_status`：当前验证状态。

外部课程中的一段方法可以被抽成 `type=knowledge`，同时保留 `source_identity=external_course`。原课程整体仍可保留 external 资产，因此来源与知识用途不冲突。

### 7.2 Family 与 Theme 分工

Family 负责“是不是同一个核心判断”，Theme 负责“这组知识共同解决什么具体问题”。每条 Claim 仍恰好进入一个 Family。

每个 Family 在 Theme 层：

- 必须恰好有一个主归属，出现在某个 Theme 的 `primary_family_ids`；
- 可以被其它 Theme 的 `family_ids` 次级引用；
- 不允许为了多主题用途复制 Claim 或 Family。

### 7.3 禁止以压缩率作为主题质量

Theme 数量没有固定目标。Review Compression 只能减少无意义的人工审核，不能成为合并不同问题域的理由。

以下情况默认优先拆分 Theme：

- 中心问题需要用“以及 / 同时 / 与 / 和”把多个独立问题绑在一起；
- 两部分知识可以分别形成独立文章、清单、判断标准、工作流或产品模块；
- 唯一合并理由只是它们都属于“个人 IP / 商业 / AI / 家庭教育”等大领域。

## 8. 确定性字段下沉

Claim 的以下字段由 Runtime 根据 Atom 自动确定：

- atom_id
- raw_id
- anchor
- statement
- evidence_status

模型只对 source_status 等语义字段做判断。

原则：能由已有结构确定的字段，不让模型重复生成。这样可以减少断行归一化、主题 ID/名称混淆、引用错位等模型实现错误。

## 9. Source Governance

Claim 阶段完成后，v0.6.3 生成：

~~~text
workflows/<wf_id>/governance/source-governance.json
~~~

每条 Claim 记录来源类型、验证状态、可信等级、使用范围和禁止推断边界。

anchor_verified 的含义只限于“锚点与 RAW 对得上”，不等于事实真实性已经被外部证明。

## 10. Coverage Audit

有 user_brief 时，Distiller 后必须形成目标覆盖审计。

每个目标包含：

- goal
- priority：critical / high / normal / low
- status：covered / partial / uncovered
- evidence_ids
- reason

critical/high 完全 uncovered 时不能继续把本轮整理当作成功交付。

## 11. Skill 晋级审计

不能通过 candidates: [] 直接结束 Skill 链。

每个 Canonical 都必须有一条审计：

- promoted
- not_skill
- needs_more_evidence
- covered_by_existing_skill

并写 reason。

只有 promoted 的 Canonical 可以被 Candidate 引用；所有 promoted 项都必须有 Candidate。

Candidate 再通过：

~~~text
来源充分性
+ 可执行性
+ 任务增益
+ 独立意图
+ 独立契约
+ 独立运行
+ 独立复用/独立评测至少一项
+ RIA++：R / I / A1 / A2 / E / B
~~~

## 12. Formal Skill

发布不再只有 metadata JSON。

通过 Gate F 后，必须生成：

~~~text
formal-skills/<formal_skill_id>/
├── SKILL.md
├── metadata.json
└── test-prompts.json
~~~

并复制到最终 Delivery Bundle 的 skills/ 下。

## 13. Delivery Contract

完成或 completed_nonknowledge 后自动生成：

~~~text
deliveries/<wf_id>/
├── manifest.json
├── README.md
├── knowledge/
├── skills/
├── 03_待办.json
├── 04_备忘.json
├── 05_文案.json
├── 06_灵感.json
├── 07_案例.json
├── 08_项目资产.json
├── 09_来源资料.json
├── 10_待整理.json
├── 11_Skill晋级审计.json
├── 12_目标覆盖审计.json
└── 13_来源治理.json
~~~

不存在的治理 sidecar 可以不生成对应文件，但 manifest 必须报告各资产数量。

## 14. 安装布局

Stage Registry 不再只认一个固定位置。会检查仓库嵌套和拍平 Skill 布局，因此安装器不应自行改写阶段路径来“适配”。

## 15. Source of Truth

优先级：

1. src/workflow-v063.js：历史文件名，当前承载 v0.7 状态、治理、发布与交付保护。
2. src/workflow.js：基础阶段顺序、基础 Gate、Artifact 和持久化。
3. schemas/*.json：结构契约。
4. src/agent-runtime.js：模型请求、确定性后处理、审计生成。
5. skills/knowledge-orchestrator/stages/*.md：阶段语义规则。
6. skills/knowledge-orchestrator/SKILL.md：对外总控行为。
7. 本文和 README。

文档与代码冲突时以 Runtime + Schema 为准。任何新能力必须先落代码/Schema/测试，再写入 Skill 说明。
