# 架构与阶段契约

## 1. 总原则

系统采用两层职责：

- **Agent / 模型**：理解语义、提出候选、形成用户可读结果。
- **WorkflowService**：负责阶段顺序、Schema、来源引用、状态持久化、Gate、审批签名和正式写入边界。

核心原则：**模型负责判断，程序负责纪律。**

对外只暴露 `skills/knowledge-orchestrator/SKILL.md`。其它阶段说明位于 `skills/knowledge-orchestrator/stages/`，不会作为独立 Skill 发现。

## 2. 主流程

```text
RAW
→ 内容分流
→ 知识拆解
→ Claim
→ Family
→ Theme
→ 关系/冲突/版本
→ 知识蒸馏
→ Proposal
→ 真人审批
→ Canonical
→ Skill Candidate
→ Skill Draft
→ 实际任务评测
→ 真人审批
→ Formal Skill Record
```

非知识资产可以在内容分流后结束；外部资料/项目案例/项目方案如果包含可复用知识，可以同时抽出 knowledge 片段继续进入知识链。

## 3. 14 个程序阶段

| 程序阶段 | 用户含义 | 关键完成条件 |
|---|---|---|
| intake | RAW 接收 | 原始字节只新增，摘要一致 |
| router | 内容分流 | RAW 非空白内容全覆盖；风险显式标记 |
| atomicizer | 知识拆解 | 每个 knowledge 至少一个 atom；允许一对多；锚点在资产范围内 |
| claims | 主张提取 | 每 atom 一 Claim，来源链不断裂 |
| family_builder | 同类知识归组 | 每 Claim 恰好一个 Family |
| theme_builder | 主题整理 | Family 全覆盖；每 Theme 有中心问题/纳入/排除 |
| reconciler | 重复/补充/冲突 | 关系端点有效；冲突/撤回有判断依据 |
| distiller | 知识蒸馏 | 每 Theme 有蒸馏结果；证据来自本 Theme |
| proposal | 正式知识提案 | 每份蒸馏形成可审 Proposal；必进 Gate D |
| canonical | 正式知识 | 只能由受保护审阅服务追加版本 |
| skill_candidate | 能力候选 | Canonical 来源真实；Triple Verify + Promotion + RIA |
| skill_builder | 能力规则构建 | 草稿绑定已批准 Candidate |
| evaluator | 实际任务评测 | 有 baseline、真实输出、paired judge |
| skill_publish | 正式能力发布 | 只能由受保护服务生成发布记录 |

## 4. Decision-First 人工审核

内部阶段不等于人工检查点。

默认自动连续执行：

```text
内容分流
→ 知识拆解
→ 同类知识归组
```

形成 Theme 后设置一次高价值**软检查点**，用户只检查高层结构是否符合本轮目标。

之后：

```text
关系检查
├─ 有真正冲突/版本取舍 → Gate C
└─ 无冲突 → 自动继续
→ 知识蒸馏
→ Proposal
→ Gate D
```

软检查点的“继续”不等于正式审批。

## 5. 六个硬 Gate

| Gate | 触发条件 | 用户真正需要判断的事 |
|---|---|---|
| A `route_risk` | router 有风险或待整理 | 路由是否可信、敏感/高风险内容如何处理 |
| B `major_theme_change` | 新核心主题、核心合并或拆分 | 高层知识结构是否应该改变 |
| C `conflict_or_version` | CONTRADICTS / RETRACTS / 版本判断 | 双方证据、适用条件、版本取舍 |
| D `canonical_proposal` | 有 Proposal | 逐条接受 / 拒绝 / 部分接受 |
| E `skill_promotion` | 有 Skill Candidate | 是否值得成为独立可调用能力 |
| F `skill_publication` | 评测建议 keep | 是否允许形成正式发布记录 |

没有触发 Gate 的正常阶段不能凭空要求用户批准。

## 6. 用户审核输出

用户侧默认采用：

1. 一句话结论；
2. 漏斗进度；
3. 相比上一阶段真正发生的变化；
4. 风险 / 冲突 / 需要决策的异常；
5. 少量代表性内容；
6. 唯一下一动作；
7. 技术详情链接。

全量逐条映射、内部 ID、hash、Artifact、来源锚点属于技术层，不作为主审核页面。

## 7. 来源身份与知识用途

资产类型回答“这是什么材料”；knowledge 链回答“其中什么值得成为长期可复用知识”。

因此一个原文片段可以同时作为项目案例来源，又成为 knowledge 资产的证据锚点。允许不同资产锚点重叠，但任何 knowledge 片段必须真实存在于 RAW。

## 8. 原子化与来源

Atomicizer 允许：

```text
1 knowledge asset → N atoms
```

每个 atom：

- 只承担一个最小可判断主张；
- anchor 必须位于所属 knowledge asset 范围；
- statement 与 anchor.quote 保持原文证据；
- 不把编辑解释伪装成原文。

Claim 再记录来源状态和证据状态。

## 9. 状态与恢复

`RUN_STATE.json` 保存：

- run 状态
- 当前阶段
- 每阶段版本与 Artifact
- Gate / Checkpoint
- 签署决定
- `user_brief`
- 软交互停点

新会话使用 `resume`，不依赖聊天记忆恢复流程。

上游被 rollback 后，下游产物标为 stale，不删除历史。

## 10. 正式写入边界

以下操作不是普通模型 `submit`：

- Canonical 写入
- 正式 Skill 发布
- 用户审批记录
- rollback

这些动作必须由 `user_review_service` 执行，并具备签名能力。推荐 Ed25519：Review Service 持私钥，Agent Runtime 只持公钥。

本地同一用户进程并不等于生产级权限隔离；正式部署仍需要独立身份认证、文件/数据库 ACL、事务与独立审计存储。

## 11. Skill 晋级与评测

知识先通过：

```text
来源充分性
+ 可执行性
+ 任务增益
```

再通过独立入口判断：独立意图、独立契约、独立运行必须通过；独立复用/独立评测至少一项通过。

候选 Skill 必须有 RIA++：R / I / A1 / A2 / E / B。

发布前评测必须有真实任务、无 Skill baseline 和 paired judge。没有真实输出或独立 judge 时，不能声称已完成正式评测。

## 12. Source of Truth

优先级：

1. `src/workflow.js` 的顺序、Gate、写入保护；
2. `schemas/*.json` 的结构契约；
3. `skills/knowledge-orchestrator/SKILL.md` 和 `stages/*.md` 的模型行为规则；
4. 本文与 README 的说明。

如果说明文档与运行代码冲突，以运行代码和 Schema 为准。
