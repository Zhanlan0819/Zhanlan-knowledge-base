# MVP 开发顺序与验收标准

> 此文档保留第一阶段的开发顺序；下文“当前 v0.1”均为当时基线，不代表最新交付状态。v0.2 的已实现约束、剩余边界和逐阶段验收，请以 [架构与阶段契约.md](架构与阶段契约.md) 与 [README.md](README.md) 为准。

## 顺序

1. RAW 与资产路由：导入文本/文件，保留原件、摘要和锚点；多资产拆件，不以整篇单标签代替。当前 v0.1 已有文本纵切与只新增文件，尚无附件/OCR/事务。
2. 知识原子与 Claim：区分“原文明确说”与“AI 推断”，保留来源状态和引用校验；给每个知识原子关联 RAW。当前实现只有精确原文原子，未实现语义改写和跨文件证据。
3. Family 与 Theme：先高精度字面预归并，再同主题内语义归并；Theme 记录中心问题、纳入/排除，父/子标签独立导航。当前只接收显式建议，不能自称已做语义聚类。
4. 关系/冲突/时间版本：关系双向可查，时间性、领域性、内在张力分开；`CONTRADICTS` 不直接合并。当前只能校验同一 run 内的建议边，跨 run 和时间版本未实现。
5. Proposal 审核与 Canonical：所有改动形成提案，真人在独立审批界面核证据、冲突与版本；正式知识只由审批事务增版。当前只生成待确认提案，无审批和正式层写入。
6. 主题级蒸馏：从已核查 Family/Theme 反查 Claim/RAW，形成有来源的原则、判断标准、案例、反例、方法和边界；防信息过度收敛。
7. Skill 孵化与评测：仓颉三重验证+独立 Skill 晋级门+RIA 能力卡，再做真实任务、baseline、独立 paired judge、keep/revert 与人工 checkpoint。当前只有结构性 gate/投票聚合，不是真实评测。

## 最低验收样例

| 样例 | 预期 | 当前状态 |
|---|---|---|
| 一段混合知识/待办/灵感/文案 | 四种资产，知识分支独立继续 | 自动测试通过（标注驱动） |
| 原文不存在或引用重复未消歧 | 拒绝拆件，不制造证据 | 自动测试通过 |
| 重复导入同一 RAW | 幂等，不覆盖；原件被改则报错 | 自动测试通过 |
| 同主题相反观点 | 两条 Claim + CONTRADICTS + 双方证据，不能自动调和 | 合约已支持同 run 建边；需增加跨 run/审校测试 |
| 一个观点但无独立执行契约 | 仍留知识库，不晋级独立 Skill | 门禁自动测试通过 |
| 三位 paired judge 多数判退步 | 输出 revert 建议，不直接覆写/删文件 | 自动测试通过 |
| 真人拒绝 Proposal | Canonical 不变，有拒绝日志与原因 | v0.2 已有签署的结构化拒绝记录、全拒绝不写 Canonical；仍缺独立身份认证与审批 UI |
| 来源被撤回或语境过期 | 保留 RAW/历史 Canonical，生成修订 Proposal | 未实现；阶段 4/5 必须补 |

## 发布前门禁

- 关键结论逐条能反查到 RAW 原文位置；原文不支持的内容不得标 `explicit`。
- 高价值主题不能只剩口号：能从蒸馏结果反查全部关键 Family、Claim、案例、反例和边界。
- 无法判断重复/冲突/版本时标“待整理/待确认”，不自动选择新观点覆盖旧观点。
- 资产目录与主题簇互不代替：跨父标签的同主题知识可相互关联，标签改名不改变主题 ID 的事实来源。
- 不得以 Schema 通过、投票数齐全或 dry-run 代替实际任务表现与人工语义复核。
- RAW 原文不可修改；需更正来源时另存新 RAW 与修订提案，不覆盖旧原文。正式知识改动须提案+真人确认；正式 Skill 发布另须测试记录、候选版本、可回退版本和用户确认。
