# v0.6 覆盖安装说明

## 安装

1. 先备份原 `knowledge-system`，或做一次 Git commit。
2. 解压本包。
3. 将包内内容复制到原项目根目录，选择**合并文件夹 + 覆盖同名文件**。不要删除原项目中本包没有包含的文件。
4. 双击：

```text
install_v06_review_architecture.bat
```

5. 完成后刷新/重启 Skill 宿主。

## 安装脚本会做什么

- 先运行单总控迁移，确保 specialist 不再以独立 `SKILL.md` 暴露；
- 给 6 个关键内部 `STAGE.md` 安装 v0.6 决策优先规则；
- 第一次修改前备份为 `.pre-v06.bak`；
- 运行后再做一次检查。

## 检查

双击：

```text
check_v06_review_architecture.bat
```

它会检查 v0.6 STAGE 补丁，并运行不依赖完整项目的轻量测试。

在你的**完整原项目**里还应运行：

```powershell
npm test
```

本覆盖包本身没有原项目的 `run.schema.json`、`skill-candidate.schema.json`、fixtures 和完整 node_modules，因此这里无法替代你本机的完整测试。

## 新任务建议

如果通过 CLI 启动，可以把本轮整理目标写到一个文本文件：

```powershell
node src/workflow-cli.js start --store my-store --input note.txt --annotations note.annotations.json --brief-file 本轮整理目标.txt
```

如果由宿主直接调用总控 Skill，总控应先把用户要求压缩为“本轮整理目标”，再持续贯穿后续阶段。

## 旧 run

v0.6 不会自动重写旧 `RUN_STATE` 或历史 Artifact。旧 run 可继续恢复；新的 decision-first 行为主要用于新建 guided run。
