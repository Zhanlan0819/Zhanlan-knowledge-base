# v0.6 真实运行复盘与修复说明

本版不是继续增加阶段，而是根据 2026-09-16 的真实知识库整理运行，重做“用户到底看什么、什么时候需要确认”。

## 真实运行暴露的问题

实际运行中出现了这些典型现象：

- 34 条主张归成 31 个知识组，真正发生合并的只有 3 组，但审阅稿仍展示完整 31 组；
- 用户需要连续回复“好的 / 已批准 / 继续 / 继续 / 继续”，很多暂停并没有明确决策对象；
- 外部资料、项目案例第一轮容易被当作“分流终点”，用户需要额外提醒系统继续提炼知识；
- 长知识资产仍按“一来源单位 → 一主张”处理，复合知识没有真正原子化；
- 同一边界（如“智慧父母属于待核验课程观点”）在多个阶段反复解释；
- 聊天里经常只说“继续推进”，真正结果放在长审阅文件中，用户需要在对话与文件之间来回切换；
- 阶段很多，但缺少稳定的收敛漏斗，用户难以快速感受到“内容越来越清晰”。

## v0.6 的核心修复

### 1. 从 Stage-First 改成 Decision-First

内部仍有 7 个阶段，但默认只保留一个软结构检查点：Theme 完成后。

真正的风险、冲突、正式写入继续由 Gate A-F 决定。

所以“阶段做完”不再自动等于“用户要回复继续”。

### 2. 审核只看变化与异常

- Family 阶段只突出真正发生合并的组；
- Reconciler 只把冲突、撤回、版本变化升级为人工决策；
- 正常重复、细化、补充、来源、应用关系自动记录；
- Proposal 只审核最终候选，不要求重新审核全部原始材料。

### 3. 持续显示收敛漏斗

用户侧会持续形成类似：

`74 条内容 → 34 条知识候选 → 47 个知识原子 → 47 条主张 → 31 个知识组 → 5 个主题 → 6 条提案`

### 4. 来源身份与未来用途分离

外部资料 / 项目案例 / 项目方案继续保留来源身份；如果其中存在可复用方法，可为具体原文片段额外建立 knowledge 资产，不再被自动放到知识链外。

### 5. 允许真正原子化

`workflow.js` 已取消“一份 knowledge asset 恰好一 atom”的硬限制。现在：

- 每个知识资产至少一个 atom；
- 一个资产可以拆多个 atom；
- atom 必须锚定在所属资产原文范围内；
- Claim 仍一一绑定 atom。

### 6. 本轮目标进入运行状态

`WorkflowService.start()` 支持 `userBrief`；CLI 支持 `--brief` / `--brief-file`。目标会进入每个阶段上下文，避免多阶段后任务重点丢失。

### 7. 内部 STAGE.md 也补丁升级

`install_v06_review_architecture.bat` 会给 Router、Atomicizer、Family、Theme、Reconciler、Distiller 的现有 `STAGE.md` 追加带标记的 v0.6 规则，并先生成 `.pre-v06.bak`。

补丁是幂等的，重复运行不会无限追加。

## 默认新交互节奏

```text
启动任务
↓
自动：内容分流 → 知识拆解 → 同类归组 → 主题整理
↓
【主题结构检查】只确认高层主题是否覆盖本轮目标
↓
自动：关系检查
├─ 真冲突/版本问题 → 人工 Gate
└─ 无冲突 → 自动知识蒸馏 → Proposal
↓
【正式知识审核】逐条接受 / 拒绝 / 部分接受
↓
受保护服务写 Canonical
```

## 保留不变的安全边界

- RAW 不覆盖；
- Artifact 版本与摘要校验继续保留；
- Gate A-F 继续保留；
- “继续”不能当成正式 Proposal 批准；
- Canonical 和正式 Skill 发布仍必须受保护签名服务执行。
