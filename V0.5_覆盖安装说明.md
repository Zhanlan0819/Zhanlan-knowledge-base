# v0.5 覆盖安装说明

## 安装

1. 建议先提交 Git 或复制一份原项目备份。
2. 将本压缩包全部内容复制到原 `knowledge-system` 根目录，选择“合并文件夹 + 覆盖同名文件”。
3. 如果之前已经完成 v0.4 单总控迁移，不需要再改 specialist 文件。
4. 如果技能列表仍显示多个 Knowledge Router/Distiller 等子 Skill，再双击 `install_v04_single_orchestrator.bat`。
5. 双击 `check_v05_interactive.bat` 做基础检查。
6. 在完整原项目中执行 `npm test` 做全量集成测试。

## 这版最重要的变化

以前：扫描/备份/索引 → 输出几个文件链接 → 看起来像“整理结束”。

现在：准备工作 → 第 1/7 阶段真实内容分流 → 展示结果 → 等待“继续” → 第 2/7 阶段…… → 第 7/7 阶段正式知识提案 → 正式审批 Gate。

## 新命令

查看中文进度和最近结果：

```powershell
node src/workflow-cli.js view --store <store> --run <wf_id>
```

只解除“等待继续”，不自动调用模型：

```powershell
node src/workflow-cli.js continue --store <store> --run <wf_id>
```

配置了模型桥接后，确认并执行到下一个暂停点：

```powershell
node src/workflow-cli.js continue-run --store <store> --run <wf_id>
```

## 注意

“继续”只表示进入下一个整理阶段，不代表批准正式知识。Proposal 写入 Canonical 仍必须经过原有签名审批。

旧 run 不会被批量迁移或重写；旧 0.2/0.3/0.4 状态继续兼容。
