# 混合内容→Proposal 等待确认：已运行样例

运行命令：在 `knowledge-system/` 执行 `npm run workflow-demo`。实际保存的一次运行是 `wf_d4b5afdd-aca5-4663-b263-da0520994fe8`；重新运行会创建另一个独立 run，不覆盖这个 run。

输入 [RAW](mixed-run/raw/raw_82192282f5968b58.txt) 包含“直播开场观点 + 下周测试待办 + 价值转译灵感 + 开场文案”。分流 [Artifact](mixed-run/workflows/wf_d4b5afdd-aca5-4663-b263-da0520994fe8/artifacts/router/v1.json) 有 4 个资产，只有知识资产继续进入 Atom/Claim/Family/Theme。原文中的非空白字符全部被资产锚点覆盖；任何拆件未定位 RAW 时阶段不会通过。

该 run 的 [RUN_STATE](mixed-run/workflows/wf_d4b5afdd-aca5-4663-b263-da0520994fe8/RUN_STATE.json) 显示 intake→distiller 已完成，proposal=`waiting_review`，Checkpoint=`canonical_proposal/waiting_user_approval`。待确认 [Proposal Artifact](mixed-run/workflows/wf_d4b5afdd-aca5-4663-b263-da0520994fe8/artifacts/proposal/v1.json) 指向 Claim `claim_4172931347899d80`，内容明确写“尚需实测”。[审计日志](mixed-run/workflows/wf_d4b5afdd-aca5-4663-b263-da0520994fe8/AUDIT.jsonl) 记录每阶段输入/输出 ID、执行者、时间与结果。

本演示**没有**接受、拒绝或部分接受 Proposal；`canonical` 和 Skill 分支均为 `not_started`，`mixed-run/canonical/` 不存在。要从新会话继续，只需使用上述 run_id 调用 `resume`，它会返回等待审批的 Gate，而不会凭聊天内容自行越级。样例的分流/蒸馏内容来自 fixture 标注适配器，不能当成独立模型语义验收结果。
