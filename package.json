{
  "name": "axing-knowledge-system",
  "version": "0.6.4",
  "private": true,
  "type": "module",
  "description": "单总控、决策优先，知识提案先进入站点待确认，再由用户确认入库的个人知识整理工作流。",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "demo": "node src/workflow-cli.js demo --store .tmp/demo --input fixtures/mixed.txt --annotations fixtures/mixed.annotations.json",
    "test:v063": "node --test tests/v063-runtime.test.js"
  },
  "dependencies": {
    "ajv": "^8.20.0"
  }
}
