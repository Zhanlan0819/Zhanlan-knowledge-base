# v0.5 交互式知识整理升级说明

## 这版解决什么

之前出现的核心问题是：扫描、备份、建立索引、生成“整理报告/离线草稿”以后就结束，用户看不到真正的知识整理结果，也不知道下一步是什么。

v0.5 将知识整理改成**7 阶段、结果可见、逐阶段继续**的 guided workflow。

## 用户看到的 7 个阶段

1. 内容分流
2. 知识拆解
3. 同类知识归组
4. 主题整理
5. 重复、补充与冲突检查
6. 知识蒸馏
7. 生成待确认提案

扫描/备份/索引是准备工作，不计入 7 个整理阶段。

## 每一步必须展示什么

每个用户阶段结束都要展示：

- 第 X/7 阶段
- 本阶段实际产出数量
- 3–8 条代表性真实结果
- 当前 7 阶段进度
- 下一阶段名称
- “回复继续”提示

技术文件、内部 ID、英文 stage、slug 默认隐藏。

## 软暂停与正式审批不同

`required_action=continue` 只是“是否进入下一整理阶段”的交互暂停，不是正式知识批准。

正式知识仍必须经过 Proposal Gate + 签名 Review Service。AI 不能因为用户说过一次“继续”就把全部 Proposal 自动写入 Canonical。

## 兼容性

- 新 guided run：schema_version 0.5.0，支持 interaction pause。
- 旧 v0.2/v0.3/v0.4 run：继续读取。
- 旧代码直接 `WorkflowService.start()`：默认 legacy，不插入软暂停。
- `workflow-cli start`：默认 guided。
- 原有硬 Gate、Ed25519、公钥验签、Artifact provenance、单总控结构全部保留。

## 覆盖后建议检查

```powershell
npm test
node src/workflow-cli.js skill-visibility
```

新增的纯展示测试也可以单独运行：

```powershell
node --test tests/display-labels.test.js tests/user-presentation.test.js
```
