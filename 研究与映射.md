# 四仓库实查与系统映射（2026-09-15）

> 本文记录第一阶段对四个参考仓库的研究与 v0.1 映射，保留当时的实现边界作为历史记录；v0.2 状态机及当前可运行范围见 [架构与阶段契约.md](架构与阶段契约.md)。本阶段没有重新研究或改写四仓库结论。

## 研究范围与证据等级

本轮 `git clone --depth 1` 下载到 `../reference-repos/`；记录的 HEAD：

| 仓库 | HEAD | 核心定位 | 当前仓库中的实现等级 |
|---|---|---|---|
| [仓颉](../reference-repos/cangjie-skill/SKILL.md) | `3adf9e61eec96edf34055432942f2952cfd7cfd9` | 长内容→可执行能力 | 阶段 0–4 大量是 Agent 工作规范；Bundle 编译、Schema、输出验证、快照/回滚有 Python 脚本 |
| [女娲](../reference-repos/nuwa-skill/SKILL.md) | `fe0374687037c4cc51a65c1e0c145afe2981dc69` | 人物/主题思维画像→Perspective Skill | 主要是 Agent 调研/提炼规范；字幕清洗、调研合并、质量检查有脚本 |
| [达尔文](../reference-repos/darwin-skill/SKILL.md) | `5539516444cff4eed7865daf61a707590acda485` | 现有 Skill→测试/优化 | paired judge、baseline、git keep/revert 主要在 SKILL.md 内编排；仓库没有通用自动 judge/回滚 CLI |
| [叁笙](../reference-repos/sansheng-distill/SKILL.md) | `66f1bc766ad3dea8fda4fbaca81810e4fe73fbbc` | 作品/创作者语料→证据型蒸馏 | 单书、网页、跨书索引、深读/传记部分有脚本；creator-corpus P0–P9 主流程主要是参考章程，公开仓库没有通用的 Family 语义归并运行器 |

“真实项目机制”表示文件中确有规范或代码，不等于本系统 v0.1 已自动执行。对应文件、位置与可运行边界逐项说明如下。我们未把四仓库安装到全局技能路径，防止触发冲突；参考源码完整保留在本交付目录。

## 1. 仓颉：保留知识与晋级 Skill 是两道门

目录实查：根 `SKILL.md`、`methodology/00–07`（含 03b 晋级门）、五份 `extractors/*.md`、`templates/`、`schemas/`、`scripts/`、`tests/`、`books/`、`benchmarks/`、`dist/`。此版本 SKILL.md 自报 v2.5 Bundle 版。

脚本/配置核查还包括 `schemas/eval-suite.schema.json`（trigger 与 output cases，后者机械断言优先）、`scripts/build_chunks.py/build_index.py`（长文本切片/检索前处理）、`scripts/validate_skill_pack.py`（产物格式/链接），以及 `templates/test-prompts.json.template`。本轮实际运行 `python scripts/cangjie.py doctor`，显示主工具/关键 Schema 可用，`jsonschema` 是可选缺失；未以 doctor 通过推断蒸馏流程 E2E 通过。

实际输入是用户提供的书、字幕、课程/访谈等长文本和元信息，不允许凭记忆蒸馏；`SKILL.md` 的“输入要求”与阶段 0 对整体骨架/关键任务做独立覆盖审计。阶段 1 五视角：框架、原则、案例、反例、术语；框架/原则全量扫描，案例/反例/术语检索式提取（`SKILL.md` 阶段 1；`methodology/02-stage1-parallel-extract.md`；`extractors/framework-extractor.md:5–36,38–70`）。候选带来源段落和缺条件，筛选发生在 1.5 而不是提取时。

`methodology/03-stage1.5-triple-verify.md:10–56` 明确 V1 来源充分、V2 可执行、V3 任务增益，分 `verified/reference/needs_review/rejected`；`coverage-audit.md` 用“原文关键任务→候选→判定→最终去向”防遗漏。`methodology/03b-stage1.6-promotion-gate.md:13–35` 的五条独立 Skill 判据是：独立意图、契约、运行三条必过，独立复用/独立评测至少过一条；未晋级仍保留为 router 能力卡，不当成知识淘汰。软预算 8 个可发现入口不是知识库硬上限。

`methodology/04-stage2-ria-plus.md:17–79` 能力卡 R 原文、I 解释、A1 案例/合成演练、A2 未来触发、E 执行、B 边界；`methodology/05-stage3-zettelkasten.md:7–38` 用 `also_read` 连能力及术语。`schemas/capability-bundle.schema.json:1–47` 把 verified Bundle 作为 single/pack 的唯一编译事实源；`scripts/compile_pack.py:57–116` 对 `promoted/router` 和去向做确定性编译。`scripts/cangjie.py:146–197,223–240` 提供编译时决策、目标目录冲突检查、快照和 rollback；CLI 文件自己说明它**不从原始书一键蒸馏**（文件头 15 行）。`methodology/06-stage4-pressure-test.md` 要测试触发、近邻负例、代表任务实际输出，router 也要验证可达且做得对。

本系统取其“先知识验证、后 Skill 产品化”和 RIA/压力测试；不把五 extractor 直接用于每一条普通输入（非知识内容会被污染），也不把未晋级知识强塞进来源路由 Skill。RAW→多资产路由是本项目适配层。

## 2. 女娲：冲突分类与诚实边界，不借人物角色扮演

目录实查：根 `SKILL.md`、`references/extraction-framework.md`、`fidelity-scorecard.md`、`skill-template.md`、`scripts/srt_to_transcript.py`、`merge_research.py`、`quality_check.py`，另有大量 `examples/*/SKILL.md/FIDELITY.md/references/research/`。没有单独 `methodology/` 目录或通用知识库 Router。

实际流程是明确人名直进/模糊诉求先诊断（`SKILL.md:30–65,69–149`），然后本地语料或多维调研、保存来源、调研 Review checkpoint、模型/启发式/表达 DNA/诚实边界提炼、确认、构建人物 Skill、质量验证、增量更新（`SKILL.md:183–240,332–425,431–595`）。脚本处理 SRT/VTT 预清洗、研究来源统计和标题式质量检查；`quality_check.py:85–102` 的一手来源占比基于关键词计数，只是启发式静态检查，不能替代事实核查。

`references/extraction-framework.md:73–108` 对矛盾分时间性演化、领域性不同规则、内在张力；不能选边忽略、编造和解；二手信息降置信，无法判定则并列。`SKILL.md:233–239,332–355,411–425` 把来源/矛盾与人工检查点前置。对本项目，借这套“冲突类型与信息不足时降级”的判断，不继承“模拟某个人”的身份/风格规则，也不把人物画像默认当知识 Canonical。

## 3. 达尔文：真实任务与 paired 比较是发布前质量门

目录实查：根 `SKILL.md`、`test-prompts.json`、`references/skilllens-evidence.md` 与 `runtime-neutrality.md`、`scripts/screenshot.mjs`、模板/视觉资源。没有通用评测 Python/Node 脚本，也没有可由本系统直接 import 的 paired judge 函数。

根 `test-prompts.json` 有典型单 Skill 优化、全量评估、歧义/失败三例，是提示/期望样例，不是自动 test runner；`runtime-neutrality.md` 检查 Claude 专属写法迁移风险；`scripts/screenshot.mjs` 服务成果卡片截图，不承担评测。

其实际指令流程：先确定范围与测试 Prompt（happy path + 歧义边界）、展示给用户确认（`SKILL.md:113–141`）；带 Skill 与无 Skill baseline 分开完成同任务，绝对 rubric 分仅粗排弱项（`143–165`）；之后每一版变更由奇数位独立 judge 在**同一次比较**里看改前/改后，用 better/worse/tie，多数决给 keep/revert，不以两次绝对分差做棘轮（`211–229`）。每支 Skill 后人工 checkpoint；回退用可追溯 `git revert`，不建议 `reset --hard`（`234–240,377–383`）。这些是 Agent 执行规范，不是自动化质量保证；没有实际输出或独立 judge 时，不得把本骨架的 `paired` 投票聚合说成 Darwin 式实测。

本系统把它放到 Skill Candidate 后。`paired` 命令仅验证奇数票/多数结果，给建议；没有自动保留、发布、回滚。正式运行应固定测试集、记录模型/环境/产物、盲评版本顺序、真人确认。RAW/Canonical 的审批不由 Darwin 优化循环替代。

## 4. 叁笙：证据索引→观点族→主题→关系→总体蒸馏

目录实查：根 `SKILL.md`、`references/creator-craft.md`、`cross-book.md`、`topic-craft.md`、`method.md`、`source-audit.md`、`biography-craft.md` 等；`scripts/` 有 `convert_book.py`、`update_index.py`、`build_topic.py`、`build_deepread_packs.py`、`verify_*` 与测试；`templates/` 有 HTML 骨架。

`.env.example` 核到 `DISTILL_DATA_DIR`，视频缺字幕时的可选 API key；网页 enrich 搜索由运行宿主工具配置，仓库内不提供一个统一 search config。`scripts/update_index.py:12–86` 是实际代码：检查五类关系，`--dry-run` 验输入，真写前拷贝 `.bak`。这与公开 creator-corpus 的七/八类关系章程必须分开看。

creator-corpus 专章 `creator-craft.md:18–40` 的三层原则是“输入全量采集、分析完整建库、展示去重重构”，L0 来源台账、L1 RAW 语料、L2 来源卡、L3 跨来源综合、L4 展示。P0–P9 实际章程（`42–56`）包括：盘点、校准小样、采集、单源卡、P5 去重/聚类/观点族/时间线、P6 系统模型/张力蒸馏、P6.5 深读、P7 外部核验、展示。P4 的锚点与来源状态、引文机械核实（`67–78`）尤其适合作为 Claim 证据纪律。

`creator-craft.md:80–94` 给出 `works.jsonl/claims.jsonl/narratives.jsonl`、Family 保留全部证据指针；字面高精度预合并 + 同主题语义归并，不能把“同话题不同主张”合并；模型只返回族 ID 分组，不改代表句。Theme 要中心问题和纳入/排除。关系类型为 `REPEATS/REFINES/EXTENDS/REBRANDS/CONTRADICTS/RETRACTS/ORIGIN/APPLICATION`，时间线区分证据变化、编辑推断、无外证因果禁止。`scripts/build_deepread_packs.py:9–18,48–78,204–227` 确实从 Family→Claim 反取材料和 provenance 防“过度收敛”。但此脚本默认从人物项目目录及网站数据位置读取文件，不是一个现成可用于任意知识库的独立程序；公开仓库亦未提供 P5 通用语义归并器。

跨书 `cross-book.md:26–63,125–139` 与 `scripts/update_index.py` 使用另一套 `NEW_CONCEPT/SUPPORTS/REFINES/CONTRADICTS/NEW_SUB_ASPECT` 枚举和 `knowledge-index.json`；`CONTRADICTS` 两边并列不抹平。它与 creator-corpus 的关系枚举**不是同一个协议**。本系统关系集优先按你指定的 creator-corpus 类型选七种；`REBRANDS` 暂不入 MVP，跨书 `SUPPORTS/NEW_SUB_ASPECT` 将来须明确映射，不静默同义替换。

叁笙单书/视频主管线包含预处理诊断、Pass1/Pass2、锚点、索引、外部 enrich、网页 verify（根 `SKILL.md` Step0–7 和三处硬门禁）。它的书籍章节质量阈值、封面/SEO/网页发布约束是该产品形态特有的，不直接移植到私人知识库。其 creator-corpus 适配的是“一个人跨媒介语料”，我们的输入则还包含案例、文案、待办等，故在 P4 前必须新增多资产 Router。

## 重组映射：先整理所有输入，再选少数知识孵化 Skill

| 模块 | 真实借鉴 | 本项目适配层/重构 | v0.1 边界 |
|---|---|---|---|
| router | 女娲 Phase0 与叁笙多路径先分流的思想 | RAW 后语义拆件，知识/案例/文案/灵感/待办/项目/外部/待整理，允许一段多路 | 标注输入驱动，非模型自动分类 |
| atomicizer | 仓颉候选来源段落；叁笙 Claim 锚点/source_status | 知识拆件原子与 RAW 一一回指；非知识不强行原子化 | 只复制精确原句 |
| family-builder | 叁笙 P5 双段归并/模型只返 ID | 跨项目/跨父标签的同主张 Family；人工可复核建议 | 仅显式 family 或精确相同归组 |
| theme-builder | 叁笙 Theme 中心问题/纳入排除 | 动态跨标签主题簇，父子标签稳定导航 | 仅显式主题，未自聚类 |
| reconciler | 叁笙关系/时间线与跨书不抹平；女娲矛盾三分 | 七类关系、冲突分类、时间版本、撤回提案 | 同 run 单向建议边，无自动消歧 |
| distiller | 叁笙 P6/P6.5 反查证据；仓颉 RIA 思考 | Theme 级知识产出 Proposal→真人审阅→Canonical | 只收外部 Proposal，未自动蒸馏 |
| skill-builder | 仓颉 V1–V3/五项晋级/RIA/Bundle | 仅成熟且适合 AI 重复执行的知识进入候选；未晋级仍是知识 | gate 做字段门禁，不编译 Skill |
| evaluator | 达尔文 prompt/baseline/paired/keep-revert；仓颉压力测试 | 与知识审批分开，候选 Skill 发布前用户 checkpoint | 只做投票汇总建议 |
| orchestrator | 仓颉阶段状态/覆盖审计；叁笙 L0–L4/校准样本 | 所有资产主系统的顺序/权限和断点；Canonical/RAW 无 AI 覆盖 | CLI 纵切，无数据库审批/任务调度 |

目标完整流：

```text
任何输入 → RAW（只新增） → 拆件与路由 → 八类资产
                                  └ 知识 → 原子/Claim → Family → Theme
                                           → 关系/冲突/时间版本 → 主题蒸馏
                                           → Proposal → 真人确认 → Canonical
                                           → 少数成熟方法 → Skill Candidate
                                           → 仓颉双门/RIA → Skill 草稿
                                           → 真实任务/baseline/paired judge
                                           → keep/revert 建议 → 真人确认 → 正式 Skill
```

父/子标签回答“去哪找”，Theme 回答“共同解决什么问题”，关系表达“知识之间如何变化”，Canonical 是当前经批准结论；四者不互相替代。所有来源、聚类、关系、冲突都是带证据的**提案**，AI 不得直接改 RAW 或 Canonical。

## 对既有机制与新设计的清楚分界

已存在于四仓库：仓颉五提取器、三重验证、独立 Skill 晋级、RIA 卡、Bundle 编译/快照；女娲多源调研、矛盾分类和人工 checkpoint；达尔文 baseline 与 paired judge 指令；叁笙 creator-corpus 的 Claim/Family/Theme/关系章程、深读反查脚本、跨书 `CONTRADICTS` 索引。

**本项目适配层**：混合输入多资产语义拆件；RAW 与 Canonical 的统一只增不改/Proposal 权限模型；八类资产 Router；跨父子标签的个人知识 Theme；将 creator-corpus 的 Family 扩展到混合项目知识库；七类关系的个人知识版本治理；真人批准 Canonical 的事务；把 Skill 当作后段可选资产；当前 Node CLI、JSON Schema 与九模块 Skill。不能把这些说成四仓库已经实现的通用知识库功能。

## 尚未实测或待补的工作

- 已下载/检视关键文件，并运行**本项目 v0.1** 的自动测试。没有在真实个人知识库语料上完成四仓库的端到端蒸馏，也没有实际运行其多 Agent 路径。
- 四份参考仓库保持干净的 Git working tree；本环境未安装 `pytest`，故未运行参考仓库自带的 pytest 测试集。此限制不影响本项目 Node 自动测试的结果，但不能用来宣称上游测试全部通过。
- creator-corpus 的核心语义分族需要自己实现/接模型并做精度/召回人工校准；主题/冲突也需要跨 run 存储和时间域证据。
- Schema 是交付契约草案；当前 CLI 有内置检查，但尚未把 JSON Schema 校验库接入运行时，也没有多文件事务、附件/OCR、备份、权限与人工审批 UI。
- Darwin 的独立 judge/真实任务运行/盲评/版本回滚尚未接入。此版只给可继续开发的门禁接口，不报告虚假的“自动优化成功”。
